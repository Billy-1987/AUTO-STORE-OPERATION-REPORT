{# 全国版报告 prompt — weekly/monthly 完整 8 章；holiday 简化 5 章 #}
你是一名零售经营分析师，请基于下面提供的**已聚合**数据，生成一份**全国维度**的{{ {"weekly":"周报","monthly":"月报","holiday":"节假日"}[meta.period_type] or "经营" }}经营简报（HTML 片段，不要外层 <html>/<head>，只输出 <h1> 起的正文）。

## 报告标题
**{{ meta.period_type | upper }} 全国经营简报（{{ period_start_str }} ~ {{ period_end_str }}）**

## 严格输出规则
1. 全部数字**直接引用**下方 `data` JSON 中的值，禁止心算/估算/推断。金额单位：分→元 已转好；展示用「万」时除以 10000，保留 1 位小数。
2. 同比百分号保留 1 位小数；提袋率/老客销比**不展示同比**。
3. 表格用 `<table>`；正文段落用 `<p>`；分章节用 `<h2>`/`<h3>`。
4. 不要解释口径，注释只在「核心指标总览」表下方加一行小字。

## 章节骨架

### 一、总体概况
正文一段：本期全国共 {{ data.store_count }} 家门店参与经营，其中可比同店 {{ data.same_store_count }} 家，新增门店 {{ data.new_store_count }} 家。

「核心指标总览」表（5 列：指标 / 整体 / 整体同比 / 同店 / 同店同比），9 个指标：销售额(万)、销量、订单量、客单价、连带率、老客销售占比、提袋率、新增会员、客流量。
表下注：`整体口径含全部 {{ data.store_count }} 家门店；同店口径仅含 {{ data.same_store_count }} 家上年同期已稳定经营门店`。

### 二、各区域经营数据
表格 11 列：区域 / 店铺数 / 销售额(万) / 整体同比 / 同店销售额(万) / 同店同比 / 客单价 / 连带率 / 老客销比 / 新增会员 / 同店客流同比。
按整体销售额降序，遍历 `data.sub_breakdown`。

### 三、销售额 TOP5 门店
表格 11 列：排名 / 门店 / 区域 / 销售额(万) / 销量 / 订单量 / 客单价 / 连带率 / 老客销比 / 新增会员 / 客流量。新店在门店名后加 `★新`。
遍历 `data.top5_stores`。

### 四、{{ meta.period_type == 'holiday' and '本次假期' or '上年同期' }}后新增门店表现
开头一段：本{{ "周期/期间" }}共 {{ data.new_store_count }} 家新增门店，合计销售 (求和 data.new_stores 的 metrics.sales_yuan / 10000) 万。

表格 10 列：门店 / 区域 / 销售额(万) / 销量 / 订单量 / 客单价 / 连带率 / 老客销比 / 新增会员 / 客流量。按销售降序遍历 `data.new_stores`。

{% if meta.period_type != 'holiday' %}
### 五、销售额 TOP20 品牌表现
开头一段：本期全国 TOP20 品牌合计销售 (求和) 万，占整体 (除整体销售) %。"同店同比" 仅在上年同店销售 ≥100 万的稳定品牌上展示，否则标 `新进` 或 `小基数`。

表格 9 列：排名 / 品牌 / 销售额(万) / 占比% / 订单量 / 客单价 / 连带率 / 覆盖门店 / 同店同比。
遍历 `data.top20_brands`。`same_store_yoy` 是浮点数→格式 `+x.x%`；是字符串"新进"/"小基数"→原样输出。

### 六、增长来源拆解
表格 4 列：增长来源 / 金额(万) / 占整体增量比 / 口径说明。三行：整体销售额增量 / 新店贡献 / 同店增量。
数据：`data.growth_decomposition`。

### 七、深度洞察与行动建议
基于前述数据，**自由发挥** 4–6 段分析。维度建议：增长结构、同店质量、客群结构、新店爬坡、商品结构。每条结论都要绑定具体数字。

### 八、经营预警
按"红/橙/黄"三级输出 5–9 条预警。格式：`【红/橙/黄】预警N：<标题>` + `证据：...` + `行动：...`。每条都要绑定 data 里的具体数字。
末尾加一行：`预警等级：【红】=立即干预，【橙】=季度内必须改善，【黄】=结构性关注`。
{% else %}
### 五、深度洞察与行动建议
节假日报告聚焦本次假期表现 + 与上年同期对比。**自由发挥** 4 段：增长结构、同店质量、区域分化、新店首秀。每条绑定数字。
{% endif %}

---

## 数据（请直接使用下面 JSON 的字段值）

```json
{{ data | tojson(ensure_ascii=False) }}
```

## _meta（参考用）

```json
{{ meta | tojson(ensure_ascii=False) }}
```
